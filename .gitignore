# --- Dependencies ---
node_modules/
package-lock.json # Optional: Remove this line if you WANT to version lockfiles (Recommended to keep lockfiles, but some prefer ignoring)
# actually, KEEP package-lock.json usually, so delete the line above if you want standard practice.
# But definitely ignore node_modules.

# --- Security (CRITICAL) ---
.env
.env.local
.env.*.local
*.pem
*.key

# --- Build Outputs (Frontend) ---
dist/
build/
out/
.next/

# --- Logs & Debugging ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# --- IDE & OS Files ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp